---

all:
  hosts:
    host1:
      ansible_ssh_host: "192.168.1.xxx"
    host2:
      ansible_ssh_host: "192.168.1.xxx"

autoReboot:
  hosts:
    host1:
    host2:

debian:
  hosts:
    host1:

ubuntu:
  hosts:
    host2:

raspberryPi:
  hosts:
    host1
