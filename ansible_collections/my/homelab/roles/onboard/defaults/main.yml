---
# defaults file for onboard

## Name of the service account Ansible will use on the remote
ansible_svc_user: ansible_svc
## Path where SSH keys generated by this role will be exported
ansible_ssh_key_dir: "./ssh_keys"

## Set to 'true' in a playbook to run the remove-ansible-svc-user.yml tasks
remove_user: false
