[dropbear]

port     = {{ jail_dropbear_port | default('ssh') }}
logpath  = {{ jail_dropbear_logpath | default('%(dropbear_log)s') }}
backend  = {{ jail_dropbear_backend | default('%(dropbear_backend)s') }}
